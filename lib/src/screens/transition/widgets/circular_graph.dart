import 'package:flutter/material.dart';

class CircularGraph extends StatelessWidget {
   
  const CircularGraph({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('CircularGraph'),
      ),
    );
  }
}